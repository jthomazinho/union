# Two modes:
#   (a) point at a known server — fill server_addr + server_fingerprint_hex
#   (b) discover via mDNS — set `discover = true` (the next two fields are ignored)
discover = false

server_addr = "127.0.0.1"
port = 24800
server_fingerprint_hex = "e57cef361aa393311bfa64c1fe50da09c56263e641e22875090e668ddc4ece86"

hostname = "smoke-test-client"
psk = "test-passphrase"
clipboard_limit_bytes = 1048576
notify_on_focus = true
sni = "union-server"
